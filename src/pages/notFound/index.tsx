import { Typography } from "@mui/material";

const NotFound = () => {
  return (
    <section className="d-flex justify-content-center align-items-center">
      <Typography variant="h3">Oops! Page is not found</Typography>
    </section>
  );
};

export default NotFound;
